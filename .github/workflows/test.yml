name: Fundraising integration
on:
  push:
  pull_request:
  # image.yml calls this workflow before it publishes an image.
  workflow_call:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: raisecontext
          persist-credentials: false
      - name: Test deployment orchestration
        run: python3 raisecontext/tests/deploy_workflow.py
      - name: Read server revision
        id: server
        shell: bash
        run: |
          revision=$(cat raisecontext/POCKETCONTEXT_VERSION)
          [[ "$revision" =~ ^[0-9a-f]{40}$ ]]
          echo "revision=$revision" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: pocketcontext/pocketcontext
          ref: ${{ steps.server.outputs.revision }}
          path: pocketcontext
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: pocketcontext/go.mod
          cache-dependency-path: pocketcontext/go.sum
      - name: Build server
        working-directory: pocketcontext
        # The flags of pocketcontext's Makefile.
        run: CGO_ENABLED=1 go build -tags sqlite_math_functions -o bin/pocketcontext ./cmd/pocketcontext
      - name: Test fundraising
        run: python3 raisecontext/tests/integration.py --binary pocketcontext/bin/pocketcontext
      - name: Test authentication and deployment settings
        run: python3 raisecontext/tests/auth.py --binary pocketcontext/bin/pocketcontext
      - name: Test Google OAuth claims and explicit provisioning
        run: python3 raisecontext/tests/oauth_integration.py --binary pocketcontext/bin/pocketcontext
      - name: Test first-start deployment settings
        run: python3 raisecontext/tests/deploy.py --binary pocketcontext/bin/pocketcontext
      - name: Test installable skill
        run: python3 raisecontext/tests/skill.py --binary pocketcontext/bin/pocketcontext
      - name: Test OAuth client
        run: python3 raisecontext/tests/oauth.py
      - name: Test Google CLI protocol
        run: python3 raisecontext/tests/oauth.py
